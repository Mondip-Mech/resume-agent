# Core LLM — NVIDIA NIM (free tier via https://build.nvidia.com)
# Uses the OpenAI-compatible client pointed at NVIDIA's endpoint
openai>=1.30.0

# Streamlit UI
streamlit>=1.40.0
pandas>=2.0.0
plotly>=5.20.0

# Web Framework (FastAPI REST API)
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.12

# Data Models
pydantic==2.9.2
pydantic-settings==2.5.2

# PDF / Document Parsing
PyMuPDF==1.24.11
pdfplumber==0.11.4
python-docx==1.1.2

# Vector Store
# On Linux/macOS: installs cleanly. On Windows: needs Visual C++ Build Tools.
# The app falls back to an in-memory TF-IDF store automatically if not installed.
chromadb>=0.5.0

# NLP — optional, falls back to regex keyword extraction
# spacy==3.7.6
# After install: python -m spacy download en_core_web_sm

# Web Scraping (for JD from URL)
httpx==0.27.2
beautifulsoup4==4.12.3
lxml>=5.2.0

# Utilities
python-dotenv==1.0.1
pyyaml==6.0.2
tenacity==9.0.0

# Neural embeddings for RAG (replaces TF-IDF similarity with semantic search)
# Downloads all-MiniLM-L6-v2 (~80 MB) on first use — cached automatically
sentence-transformers>=2.7.0

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
